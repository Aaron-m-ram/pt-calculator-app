{
  "name": "src",
  "version": "1.0.0",
  "description": "",
  "main": "Form.js",
  "scripts": {
    "test": "jest --watchAll --env=jsdom",
    "applesauce": "jest --env=jsdom",
    "start": "http-server ./",
    "dev-start": "git fetch && git pull origin master && npm install",
    "coverage": "jest --coverage --watchAll --env=jsdom",
    "coverage-open": "open ./coverage/lcov-report/index.html",
    "git-push": "git add . && git commit -m $npm_config_message && git push origin $npm_config_branch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@babel/cli": "^7.14.8",
    "@babel/core": "^7.14.8",
    "@babel/preset-env": "^7.14.7",
    "eslint": "^7.31.0",
    "jest": "^27.0.6",
    "prettier": "2.3.2"
  },
  "dependencies": {
    "lodash": "^4.17.21",
    "moment": "^2.29.1"
  }
}
